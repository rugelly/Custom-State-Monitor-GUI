namespace CustomStateMachine
{
    public static class FilePaths
    {
        public const string root        = "Custom State Machine/";
        public const string state       = root + "State";
        public const string actions     = root + "Action/ ";
        public const string connection  = root + "Connection/ ";
    }
}
